export type SliderItemPropsType = {
  image: {
    imageUrl: string,
    height: number,
    width: number,
  }
  text: string,
};

export type InfoCardPropsType =  {
  title: string,
  description: string[],
}